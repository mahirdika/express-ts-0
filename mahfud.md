<h3> ts-node module
> Jembatan agar kita bisa tes kode tanpa compile manual.

<h3> typescript
> Mesin yang mengubah .ts menjadi .js.

<h3> @types/express & @types/cors
> semacam kamus untuk memberitahu typescript tentang perintah express dan cors

<h3> eslint
> ESLint ini bertugas memastikan kode kamu rapi, mengikuti standar industri, dan tidak memiliki potensi bug logis.

<h3> @eslint/js
> Memberikan daftar aturan dasar (konfigurasi rekomendasi) tentang apa yang boleh dan tidak boleh dilakukan di JavaScript.

<h3> typescript-eslint
ESLint aslinya hanya mengerti JavaScript. Ia menjadi jembatan agar ESLint bisa mengerti fitur-fitur khusus TypeScript (seperti interface, enum, atau type). -> parser

<h3> prettier configuration
https://prettier.io/docs/configuration